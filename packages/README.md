# Packages

Shared D3HR packages.

## Planned Packages

- `database`: schema, migrations, query helpers
- `policy`: access-policy contracts and evaluation
- `audit`: audit event contracts
- `shared-types`: shared domain types
- `ui`: shared design system components

